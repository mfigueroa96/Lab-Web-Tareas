var assert = require('assert');

describe('Login', () => {
    before(() => {
        console.log('Signup test running...');
    });
    
    it('Successfull Login', () => {
        browser.url('/');
        var _;
    });

    after(() => {

    });
});